# 官網繁中展示實作工作

- [x] 1. 從 Git 歷史復原繁中主畫面，建立網站 PNG／AVIF／WebP 素材並驗證尺寸。
- [x] 2. 更新官網畫面區的三張卡片、文案、替代文字與桌機／行動版配置。
- [x] 3. 執行 repository、網站靜態與三個 viewport 瀏覽器驗收。

## 驗收條件

- 情境：桌機開啟畫面區，左側英文主畫面跨兩列，右側依序顯示繁中主畫面與語言設定。
- 情境：平板或手機開啟畫面區，依英文、繁中、設定順序堆疊且不裁掉關鍵 UI。
- 情境：看到繁中卡片時，文案清楚說明需使用支援 CJK 的 Nexus 字型，不暗示舊圖包含新版 Location 排序狀態。
- 情境：現代瀏覽器選用 AVIF／WebP，PNG fallback 存在，所有圖片都有正確尺寸與替代文字。
- 情境：新增圖片保持首屏以下 lazy loading，首屏圖片預算不增加。
